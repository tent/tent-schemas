title: Tent Profile Post
type: object
properties:
  types:
    description: The types of profile info that have been modified.
    type: array
    required: true
    items:
      type: string
      format: uri
  action:
    description:
      The action that was performed on the profile info. One of `create`,
      `update`, or `delete`.
    type: string
    required: true
