title: Tent Follower Post
type: object
properties:
  id:
    description: The identifier of the follower.
    type: string
    required: true
  entity:
    description: The entity URL of the follower.
    type: string
    format: uri
    required: true
  action:
    description: The action performed on the follower. One of `create` or `delete`.
    type: string
    required: true
