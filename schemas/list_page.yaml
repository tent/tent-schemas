title: List Page
type: object
additionalProperties: false
properties:
  pages:
    description: Links to other pages. Each link contains a relative URL with only query parameters, starting with `?`.
    type: object
    required: true
    additionalProperties: false
    properties:
      first:
        description: Link to the first page. Omitted if the current page is the first page.
        type: string
        format: uri
      prev:
        description: Link to the previous page. Omitted if the current page is the first page.
        type: string
        format: uri
      next:
        description: Link to the next page. Omitted if the current page is the last page.
        type: string
        format: uri
      last:
        description: Link to the last page. Omitted if the current page is the last page.
        type: string
        format: uri
  data:
    description: The list page data.
    type: array
    required: true
    items:
      type: object
      additionalProperties: true
